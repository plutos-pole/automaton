const WIDTH  = 600
const HEIGHT = WIDTH
const COLS   = 30
const ROWS   = COLS
const CELL_DIMENSION = Math.floor(WIDTH / COLS)
const FPS = 10
const FRAME_TIME = 1000 / FPS
const COLOR_BLACK = `0,0,0`

export {
	WIDTH,
	HEIGHT,
	COLS,
	ROWS,
	CELL_DIMENSION,
	FRAME_TIME,
	COLOR_BLACK
}